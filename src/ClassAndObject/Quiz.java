package ClassAndObject;

public class Quiz {
    public static void main(String[] args) {
        // 1 D array
        // 1 calculate the maximum value of an array
//        int[] array = {1, 3, 5, 6, -1};
//        int max = maxValue(array);
//        System.out.println(max);
        // 2 Given an array, and an element to inset, and the position to insert this element, return a new array with the element inserted
//        int[] arr = {1, 3, 5, 7};
//        int item = 5, index = 4;
//        int[] newArr = insertArr(arr,item,index);
//        System.out.println(Arrays.toString(newArr)); //Arrays
        // 3 reverse an array
//        int[] arr = {2, 3, 8, 4, 1};
//        reverse(arr);
//        System.out.print(Arrays.toString(arr));

    }
    // 1 D array
    // 1 calculate the maximum value of an array
//    public static int maxValue(int[] arr) {
//        int max = arr[0];
//        for (int i = 0; i < arr.length; i++) {
//            if (arr[i] > max) {
//                max = arr[i];
//            }
//        }
//        return max;
//    }
    // 2 Given an array, and an element to inset, and the position to insert this element, return a new array with the element inserted
//    public static int[] insertArr(int[] arr, int item, int index) {
//        int[] newArr = new int[arr.length + 1];
//        for (int i = 0; i < index; i++) {
//            newArr[i] = arr[i];
//        }
//        newArr[index] = item;
//        for (int j = index; j < arr.length; j++) {
//            newArr[j + 1] = arr[j];
//        }
//        return newArr;
//    }
    // 3 reverse an array
//    public static void reverse(int[] arr) {
//        for (int i = 0, j = arr.length - 1; i < j; i++, j--) {
//            int temp = arr[i];
//            arr[i] = arr[j];
//            arr[j] = temp;
//        }
//    }

}
